# instead of dict, maybe a local sqlite3 database?
# probably overkill for a database with only 2 elements
# querying would be easier though
# uri would be a primary key
# poor performance likely, but we're not really going for performance
# 
import sqlite3 as sql
